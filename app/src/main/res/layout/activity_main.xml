<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/background_dark">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Stereo Widener"
            android:textColor="@color/on_surface"
            android:textSize="26sp"
            android:textStyle="bold"
            android:layout_marginStart="4dp"
            android:layout_marginBottom="4dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Plays in the background — controllable from the notification"
            android:textColor="@color/on_surface_muted"
            android:textSize="12sp"
            android:layout_marginStart="4dp"
            android:layout_marginBottom="20dp" />

        <!-- Now Playing -->
        <com.google.android.material.card.MaterialCardView style="@style/SectionCard">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <TextView style="@style/SectionTitle" android:text="Now Playing" />

                <TextView
                    android:id="@+id/tvTrackName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="No track loaded"
                    android:textColor="@color/on_surface"
                    android:textSize="15sp"
                    android:gravity="center"
                    android:layout_marginBottom="16dp"
                    android:ellipsize="middle"
                    android:singleLine="true" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnPickFile"
                    style="@style/Widget.Material3.Button.TonalButton"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:text="Choose Audio File" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnPlayPause"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:layout_marginTop="12dp"
                    android:text="Play"
                    android:enabled="false" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Stereo Width -->
        <com.google.android.material.card.MaterialCardView style="@style/SectionCard">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <TextView style="@style/SectionTitle" android:text="Stereo Width" />

                <TextView
                    android:id="@+id/tvWidthLabel"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="150%"
                    android:textColor="@color/primary"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:gravity="center" />

                <com.google.android.material.slider.Slider
                    android:id="@+id/sliderWidth"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:valueFrom="0"
                    android:valueTo="250"
                    android:value="150"
                    app:thumbColor="@color/primary"
                    app:trackColorActive="@color/primary"
                    app:trackColorInactive="@color/surface_dark_alt" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="0% mono · 100% original · 250% maximum wide"
                    android:textSize="11sp"
                    android:textColor="@color/on_surface_muted"
                    android:gravity="center" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Spectrum -->
        <com.google.android.material.card.MaterialCardView style="@style/SectionCard">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <TextView style="@style/SectionTitle" android:text="Spectrum" />

                <Spinner
                    android:id="@+id/spinnerVisualizerStyle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp" />

                <com.example.stereowidener.SpectrumVisualizerView
                    android:id="@+id/spectrumView"
                    android:layout_width="match_parent"
                    android:layout_height="120dp"
                    android:background="@color/surface_dark_alt" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Equalizer -->
        <com.google.android.material.card.MaterialCardView style="@style/SectionCard">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <TextView style="@style/SectionTitle" android:text="48-Band Equalizer" />

                <com.example.stereowidener.EqualizerView
                    android:id="@+id/eqView"
                    android:layout_width="match_parent"
                    android:layout_height="180dp"
                    android:layout_marginBottom="12dp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnResetEq"
                    style="@style/Widget.Material3.Button.TextButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Reset EQ" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

    </LinearLayout>
</androidx.core.widget.NestedScrollView>
